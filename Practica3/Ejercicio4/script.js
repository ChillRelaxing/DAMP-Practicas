 // Crea un array con algunos números
 let numbers = [10, 20, 30, 40, 50];

 // Inicializa una variable para almacenar la suma total
 let totalSum = 0;

 // Recorre el array y suma cada elemento a la variable totalSum
 for (let i = 0; i < numbers.length; i++) {
     totalSum += numbers[i];
 }

 // Muestra la suma total en la consola
 console.log("La suma total de los elementos es: " + totalSum);