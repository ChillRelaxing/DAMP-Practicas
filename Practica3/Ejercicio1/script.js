// Inicializa un array vacío
let numbers = [];

// Utiliza un bucle for para agregar números del 1 al 10 al array
for (let i = 1; i <= 10; i++) {
    numbers.push(i);
}

// Muestra el array en la consola
console.log(numbers);